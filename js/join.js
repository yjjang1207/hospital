window.onload = function(){
    /*
    let uid = document.getElementById('u_id')
    let pwd = document.getElementById('u_pwd')
    let repwd = document.getElementById('u_repwd')
    let uname = document.getElementById('u_name')
    let tel = document.getElementById('tel')
    let email = document.getElementById('email')


    if(uid.value == ""){
        alert("아이디를 입력하세요.");
        uid.focus();
        return false;
    }
    let id_len = uid.value.length;
    if(id_len < 4 || id_len > 12){
        alert("아이디는 4~12글자만 입력할 수 있습니다.");
        uid.focus();
        return false;
    }
    
    if(!pwd.value){
        alert("비밀번호를 입력하세요.");
        pwd.focus();
        return false;
    }
    let pw_len = pwd.value.length;
    if(pw_len < 8){
        alert("비밀번호는 최소 8글자 이상 입력할 수 있습니다.");
        pwd.focus();
        return false;
    }
    if(pwd.value != repwd.value){
        alert("비밀번호가 일치하지 않습니다.");
        repwd.focus();
        return false;
    }

    if(uname.value == ""){
        alert("이름을 입력하세요.");
        uname.focus();
        return false;
    }
    */

}